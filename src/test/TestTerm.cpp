#include <iostream>
#include "../Term.hpp"

using namespace PROJECT;

int main(int argc, char *argv[] )
{
    ConstTable table;

    /*
    ConstRef c1 = table.addConst("kalle");
    ConstRef c2 = table.addConst("kula");
    ConstRef c3 = table.addConst("testar");

    table.print(std::cout);
    */

    return 0;
}
